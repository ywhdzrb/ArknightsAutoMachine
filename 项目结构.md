src/
├── AMA/
│   ├── L0/                    # 感知执行层
│   │   ├── __init__.py
│   │   ├── sensor.py          # ADB截图封装 (AdbSensor类)
│   │   ├── motor.py           # ADB输入封装 (AdbMotor类)
│   │   └── bridge.py          # L0与上层通信接口
│   │
│   ├── L1/                    # 反射层 (独立线程)
│   │   ├── __init__.py
│   │   ├── reflex_arc.py      # ReflexArc主类
│   │   ├── triggers/          # 触发器插件
│   │   │   ├── hp_monitor.py  # 血线监控
│   │   │   └── emergency.py   # 紧急撤退
│   │   └── actions/           # 反射动作
│   │       ├── retreat.py
│   │       └── skill_trigger.py
│   │
│   ├── L2/                    # 映射层
│   │   ├── __init__.py
│   │   ├── mapper.py          # CoordinateMapper主类
│   │   ├── levels/            # 关卡配置 (JSON)
│   │   │   ├── 1-7.json
│   │   │   └── CE-6.json
│   │   └── resolvers/         # 分辨率适配器
│   │       ├── 1920x1080.py
│   │       └── 2560x1440.py
│   │
│   ├── L3/                    # 预测层
│   │   ├── __init__.py
│   │   ├── predictor.py       # EnemyPredictor主类
│   │   ├── trackers/          # 跟踪算法
│   │   │   ├── optical_flow.py
│   │   │   └── template_matcher.py
│   │   └── models/            # 轻量预测模型 (非NN)
│   │       └── linear_extrapolation.py
│   │
│   ├── L4/                    # 记忆/状态层
│   │   ├── __init__.py
│   │   ├── state_machine.py   # CombatStateMachine
│   │   ├── memory_db.py       # SQLite封装
│   │   ├── snapshot.py        # 对局快照结构
│   │   └── history.py         # 操作历史记录
│   │
│   └── L5/                    # 决策层
│       ├── __init__.py
│       ├── brain.py           # VisionEngine主类
│       ├── prompts/           # VLM提示词模板
│       │   ├── analyze.txt    # 战场分析提示词
│       │   └── decide.txt     # 决策生成提示词
│       └── parsers/           # 响应解析器
│           └── json_extractor.py
│
├── common/                    # 基础设施层 (跨层共享)
│   ├── adb/                   # ADB底层封装
│   │   ├── client.py          # adb-shell包装
│   │   └── utils.py           # 设备发现/连接
│   ├── cvscan/                # 计算机视觉基础
│   │   ├── detector.py        # 通用目标检测接口
│   │   └── ocr.py             # 文字识别 (费用/COST)
│   ├── yolo/                  # 本地检测模型 (可选加速)
│   │   └── detector.py        # YOLOv8推理封装
│   └── bus.py                 # 层间消息总线 (关键)
│
├── gui/                       # 新增：界面层
│   ├── __init__.py
│   ├── app.py                 # ttk主应用
│   ├── panels/
│   │   ├── preview.py         # 实时画面预览
│   │   ├── control.py         # 手动控制面板
│   │   ├── config.py          # 配置编辑器
│   │   └── monitor.py         # 性能监控(L0-L5延迟)
│   └── widgets/
│       └── level_editor.py    # 关卡标定工具
│
├── config/                    # 新增：全局配置
│   ├── default.yaml           # 默认配置
│   └── user.yaml              # 用户配置(gitignore)
│
├── tests/                     # 新增：测试
│   ├── test_L0.py             # 截图/点击测试
│   ├── test_L2_mapping.py     # 坐标映射精度测试
│   └── integration/           # 集成测试
│       └── test_full_loop.py
│
└── main.py                    # 入口文件